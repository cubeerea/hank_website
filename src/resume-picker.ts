/**
 * The résumé picker is static markup — this entry exists only so Vite bundles
 * the stylesheet for the page. If it ever needs behaviour, note that the rows
 * must keep working without it.
 */

import './style.css';
